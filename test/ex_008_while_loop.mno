# While loop counter.
fn countdown(n: int) -> int {
    let c = 0
    let i = n
    while i > 0 {
        c = c + 1
        i = i - 1
    }
    return c
}

fn main() {
    print(countdown(5))
}

test "countdown" {
    assert countdown(5) == 5
    assert countdown(0) == 0
}
