# Bounds-safe array get.
fn get_or(xs: [int], i: int, d: int) -> int {
    if i < 0 || i >= len(xs) {
        return d
    }
    return xs[i]
}

fn main() {
    print(get_or([1, 2], 5, 99))
}

test "get_or" {
    assert get_or([1, 2], 1, 99) == 2
    assert get_or([1, 2], 5, 99) == 99
}
