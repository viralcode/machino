# Generic Num bound add.
fn<T: Num> twice(x: T) -> T {
    return x + x
}

fn main() {
    print(twice(21))
    print(str_of_float(twice(1.5), 1))
}

test "twice" {
    assert twice(21) == 42
    assert twice(1.5) == 3.0
}
