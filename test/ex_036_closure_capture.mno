# Closure capture by value.
fn make_adder(n: int) -> fn(int) -> int {
    return fn(x: int) -> int {
        return x + n
    }
}

fn main() {
    let add5 = make_adder(5)
    print(add5(10))
}

test "closure" {
    let add5 = make_adder(5)
    assert add5(10) == 15
}
